services:
  review-app:
    build: .
    container_name: leetcode-review
    ports:
      - "8010:8000"
    volumes:
      - review-data:/app/data
    restart: unless-stopped

volumes:
  review-data:
